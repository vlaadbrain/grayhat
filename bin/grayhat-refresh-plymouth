#!/bin/bash

if [[ "$1" == "-y" ]] || gum confirm "Refresh Plymouth theme? This will replace your current startup screen with Grayhat defaults."; then
  sudo cp ~/.local/share/grayhat/default/plymouth/* /usr/share/plymouth/themes/grayhat/
  sudo plymouth-set-default-theme -R grayhat
fi
